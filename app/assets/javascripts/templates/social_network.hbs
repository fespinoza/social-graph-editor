<h1>{{name}}</h1>

<svg id="graph_canvas" height="500" width="700" style="border: 1px solid black">
</svg>

<ul class="actor_list">
  {{#each actor in actors itemController="actor"}}
    {{view App.ActorView contentBinding="actor"}}
  {{/each}}
</ul>
